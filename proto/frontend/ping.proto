syntax = "proto3";

package frontend;

message PingRequest {
    uint64 id = 1;
}

message PingResponse {
    uint64 id = 1;
}

service PingService {
    rpc Ping(PingRequest) returns (PingResponse) {}
}
